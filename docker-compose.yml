# docker-compose.yml

services:
  client:
    build:
      context: ./client
    container_name: template-ui
    ports:
      - 3000:80
    volumes:
      - ./client/public/config:/usr/share/nginx/html/config:ro
